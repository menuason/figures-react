import { Header } from './header';

import './page-shell.scss';

export const Page = () => {

  return (
    <div className="Page">
      <Header />

      {/*<PointPage showForm={showForm} hideForm={handleHideForm} />*/}
    </div>
  );
};
